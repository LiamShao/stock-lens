interface:
  display_name: "Handoff"
  short_description: "Compact this conversation for another agent"
  default_prompt: "Use $handoff to prepare a concise handoff for the next agent session."
policy:
  allow_implicit_invocation: false
